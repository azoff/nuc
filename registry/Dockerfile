FROM registry:2

ENV REGISTRY_AUTH htpasswd
ENV REGISTRY_AUTH_HTPASSWD_REALM Registry Realm

ARG REGISTRY_AUTH_HTPASSWD_PATH=/etc/registry.htpasswd
ENV REGISTRY_AUTH_HTPASSWD_PATH=$REGISTRY_AUTH_HTPASSWD_PATH
